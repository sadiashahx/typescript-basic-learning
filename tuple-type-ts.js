// Tuple Type
// Tuple like array but some advanced 
// array
// Example
var role = ['admin', 'manager'];
var rolee = ['admin', 'manager'];
// let roleee:string[] | number[] = ['admin', 'manager', 21];                        // string and num both vaues not assigned
// tuple -> length limit kerdega, and ye bataega ke first element kis tarah ka hoga, second kis tarah ka etc
// Example
var roles = ['admin', 'manager', 21]; // boolean? -> if boolean he tou show
roles.push(true);
roles[1] = 'hey tuple';
// roles[1] = 21                                                                    // wrong
